#include <stdio.h>
#include <math.h>
int main()
{
	int n;
	float sqt;
	printf("\nEnter any number to find square root :");
	scanf("%d",&n);
	sqt=sqrt(n);
	printf("square of a given number %d is %f",n,sqt);
}
